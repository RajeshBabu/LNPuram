var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/', function(req, res) {
  res.render('familytree', { title: 'NETAJI EMPLOYEE UNION (Lakshminarayana Puram)' });
});

module.exports = router;
